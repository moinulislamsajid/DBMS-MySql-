-- now using default

-- create table

 /* CREATE TABLE defaultCheck (
    Roll INT(5) NOT NULL AUTO_INCREMENT,
    NameIS VARCHAR(222) NOT NULL,
    Gender VARCHAR(11) NOT NULL,
    City VARCHAR(110) DEFAULT 'Dhaka',
    DOF DATE , 
    PRIMARY KEY (Roll)
);
use d74;
*/

select *
from defaultcheck;
